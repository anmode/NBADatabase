
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Import Faculty data from CSV File to mongoDB</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <form action="/" method="post" enctype="multipart/form-data">
      <div class="mb-3">
        <input type="file" class="form-control" name="csvFile" />
      </div>
      <button type="submit" class="btn btn-success">Upload</button>
      <%if(data){%>
      <div class="container mt-4">
        <table class="table">
          <thead>
            <tr>
              <th>srNum</th>
              <th>Name</th>
              <th>Email</th>
              <th>Designation</th>
              <th>Mobile</th>
            </tr>
          </thead>
          <tbody>
            <%for(var i=0; i < data.length; i++){%>
            <tr>
              <td><%= i+1%></td>
              <td><%= data[i].srNum%></td>
              <td><%= data[i].name%></td>
              <td><%= data[i].empID%></td>
              <td><%= data[i].PAN%></td>
              <td><%= data[i].adhaarNum%></td>
              <td><%= data[i].highestDegree%></td>
              <td><%= data[i].degreeDate%></td>
              <td><%= data[i].specialization%></td>
              <td><%= data[i].currentDesignation%></td>
              <td><%= data[i].dateOfDesignationAsProff%></td>
              <td><%= data[i].dateOfJoining%></td>
              
            </tr>
            <%}%>
          </tbody>
        </table>
      </div>
      <%}%>
    </form>
  </body>
</html>